const Cart = () => {
  const title = "Your Cart";
  // need to keep track of state
  // need storage
  // remember to set the default products to empty array not
  // null and so do not set the state to the items unless
  // we have items after parsing json, if the localstorage
  // doesn't have valid data to be parsed as json we can
  // do try catch
  return (
    <div></div>
  );
};

export default Cart;